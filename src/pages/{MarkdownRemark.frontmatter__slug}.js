import * as React from "react";
import { graphql } from "gatsby";

// export default function MarkdownTemplate({ data }){

//     const { allMarkdownRemark: { nodes: pages} } = data;
//     pages.forEach(page => {
//         const { frontmatter, excerpt, timeToRead } = page;
//         return (
//         )
//     })

// }
